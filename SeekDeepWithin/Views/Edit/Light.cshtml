@using SeekDeepWithin.Pocos
@model SeekDeepWithin.Pocos.Light
<div class="small-12 medium-4 large-3 column end">
   <div class="callout" id="editLight@(Model.Id)">
      @(Model.Text)
      <div>
         <select id="editLightType@(Model.Id)">
            @{ var truthTypes = Enum.GetValues (typeof (SdwType)).Cast<SdwType> ().ToList (); }
            @foreach (var truthType in truthTypes) {
               <option value="@((int)truthType)">@(truthType.ToString ())</option>
            }
         </select>
      </div>
   </div>
</div>