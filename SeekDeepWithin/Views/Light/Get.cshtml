@model IEnumerable <SeekDeepWithin.Pocos.Light>
@foreach (var light in Model) {
   <div class="small-6 medium-4 large-3 column">
      <div class="callout" onclick="sdw.sel(@(light.Id));" id="l@(light.Id)">@(light.Text)</div>
   </div>
}
